services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    image: credix-backend:local
    container_name: credix-backend
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: ${PORT:-3000}
    ports:
      - '${PORT:-3000}:3000'
